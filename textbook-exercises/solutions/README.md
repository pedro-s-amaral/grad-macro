# Textbook Exercise Solutions

This folder contains the ECON 503 textbook exercise solution source files that are included in the public repository.

The `.qmd` files are Quarto source documents. They use `style_C.css`, which is the companion CSS stylesheet for rendering these solution files.

Rendered `.html` files are intentionally not tracked. Re-render the `.qmd` sources locally when needed.
